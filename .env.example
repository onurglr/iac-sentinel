# Copy this file to `.env` and fill in real values. `.env` is gitignored.
#
# GitHub token with the "Models: read-only" permission.
# Create at: https://github.com/settings/tokens  (fine-grained token)
GITHUB_TOKEN=
